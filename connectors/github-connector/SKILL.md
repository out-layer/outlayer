---
name: github-connector
description: Work in the owner's GitHub account through the `github` connector — issues, comments, files, commits, pull requests, reviews and gists, under the owner's policy, all under the owner's name. Use when an agent with an OutLayer wallet needs to act on GitHub as its owner.
---

# GitHub connector

The skill an agent reads is published at

**https://skills.outlayer.ai/github-connector/SKILL.md**

It is served as plain text, so an agent can fetch it directly. How any
connector is called — auth, the `operation` field, secrets, fees, refusal
codes — is in the library skill:
https://skills.outlayer.ai/outlayer-connectors/SKILL.md
